{
  "name": "cloud-itonami-kintai",
  "private": true,
  "type": "module",
  "scripts": {
    "build-edge": "amu compile --target wasm32-browser edge-api",
    "deploy": "wrangler pages deploy public --project-name=cloud-itonami-kintai"
  },
  "devDependencies": { "shadow-cljs": "^2.28.0" }
}
